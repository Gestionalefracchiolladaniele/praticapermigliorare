# BizQuery — dipendenze

# --- Livello 1 (v0) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
psycopg[binary]==3.2.3
python-dotenv==1.0.1
google-genai==0.8.0
pydantic==2.11.9

# --- Livello 2 (grafo agenti) ---
langgraph==1.2.7

# --- Livello 2 (observability / tracing) ---
# Langfuse v4 (SDK su OpenTelemetry). Il CallbackHandler (langfuse.langchain)
# aggancia il grafo LangGraph: LangGraph invoca i callback a ogni nodo e Langfuse
# costruisce trace(=run) + span(=nodo). Il handler v4 legge chiavi/host dal client
# Langfuse globale, che inizializziamo in observability/langfuse_setup.py.
# `langchain` è richiesto dall'integrazione (langchain-core arriva come dipendenza).
# Versioni verificate compatibili coi pin sopra in Python 3.12 (nessun conflitto).
langfuse==4.13.2
langchain==1.3.12

# --- Livello 3 (tool come server MCP) ---
mcp==1.28.1

# dev / test
pytest==8.3.4
httpx==0.28.1
# La suite di regressione di sicurezza (tests/test_security_redteam.py) legge i
# casi red-team da security/promptfooconfig.yaml — la stessa config usata da
# promptfoo. PyYAML serve per parsarla.
pyyaml==6.0.2
